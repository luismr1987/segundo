pipeline {
    agent any

    stages {
        stage('push') {
            steps {
                echo 'Hello World'
                echo "rama de git ${env.BRANCH_NAME}"
                echo "rama" + env.BRANCH_NAME
            }
        }
    }
}
